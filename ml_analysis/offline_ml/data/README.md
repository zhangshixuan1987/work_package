# External input data

Input datasets are not committed here. Users should provide external input
directories through script arguments or notebook configuration cells.

Legacy offline-evaluation inputs have been relocated to the external `data/`
directory documented in `../configs/paths.toml`.

