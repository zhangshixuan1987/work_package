#!/bin/bash

#echo ========================================================
#i=10
#echo Attempting to restart ${i}
#date
#ensnam=EN`printf "%02d" ${i}`
#my_casename="${my_compset}_${my_resolution}_${ensnam}_${my_machine}"
#run_script="${my_wkdir}/scripts/${my_casename}.sh"
#run_refdir="${my_runpath}/${my_casename}/archive/rest/${my_dartdate}-${my_darttod}"
#cd ${my_runpath}/${my_casename}/cases
#./xmlchange run_exe="--kill-on-bad-exit=1 --job-name=${n} \${EXEROOT}/e3sm.exe "
#./xmlchange STOP_N="6"
#./case.submit --no-batch 2>&1 > ../log.o${SLURM_JOB_ID} &
#PID=$!
#echo $PID
#echo ========================================================

